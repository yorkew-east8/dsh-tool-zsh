// The dsh-zsh-bundle is a patch-only bundle: the Loader reads its
// dsh.bundle.patch (cordis.patch.yml). This module is never imported as a
// plugin; it exists only so package.json has a resolvable main.
export default {};
